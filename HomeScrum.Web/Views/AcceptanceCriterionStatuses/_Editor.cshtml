@using HomeScrum.Web.Extensions

@model HomeScrum.Web.Models.Admin.AcceptanceCriterionStatusEditorViewModel

@using (Html.BeginForm( "Edit", "AcceptanceCriterionStatuses", new { id = Model.Id }, FormMethod.Post, new { id = "Editor" } ))
{
   @Html.ValidationSummary( true )

   @Html.Partial( "_SystemDomainObjectEditor", Model );

   <fieldset>
      <legend>Properties</legend>

      <div class="formSelectList">
         @Html.LabelFor( model => model.Category )
         @Html.EnumDropDownListFor( model => model.Category )
         @Html.ValidationMessageFor( model => model.Category )
      </div>

      <div class="formBooleanOption">
         @Html.CheckBoxFor( model => model.AllowUse, new { @class = "MainData" } )
         @Html.LabelFor( model => model.AllowUse )
         @Html.ValidationMessageFor( model => model.AllowUse )
      </div>
   </fieldset>

   @Html.HiddenFor( model => model.Mode );

   <p>
      <input type="submit" value="@ViewBag.EditorButtonLabel" />
   </p>
}

<p>
   <a href="#" id="SubmitButton">TODO: blank me, remove other</a>
</p>

@Scripts.Render( "~/Bundles/BaseScripts" )
<script>
   $(document).ready(function () {
      EditorBase.init();
   });
</script> 